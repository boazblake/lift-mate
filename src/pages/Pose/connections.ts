// connections.ts
export const POSE_CONNECTIONS: [number, number][] = [
  // Replace with actual connections as per MediaPipe's Pose Landmarker
  [0, 1],
  [1, 2],
  [2, 3],
  // ... add all necessary connections
];

// Make it globally accessible
window.POSE_CONNECTIONS = POSE_CONNECTIONS;
